# Database
spring.datasource.url=jdbc:mysql://localhost:3306/rshop_test?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true #Fix for PostgreSQL

# JPA Show SQL
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

# Security
security.oauth2.client.client-id=rshop
security.oauth2.client.client-secret=rshopsecret
jwt.secret=MY-JWT-SECRET
jwt.token.duration=900
jwt.refresh-token.duration=86400